linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - misspell

run:
  timeout: 5m
  go: "1.22"

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
