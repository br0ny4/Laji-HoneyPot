feat: 前后端日志系统 — 请求追踪+状态栏调试面板

后端:
- 新增 requestLogMiddleware 请求日志中间件，记录 method/path/status/duration/remote
- 新增 responseWriter wrapper 捕获写入状态码
- main.go 启动流程补充 plugins started 里程碑日志

前端:
- api.ts 重构: 新增 LogEntry 类型、onLog() 订阅、emitLog() 发射
- apiFetch 自动记录每次请求的 status+耗时 (GET /api/stats → 200 (3ms))
- 新增 StatusBar 组件 — 页面底部固定调试栏:
  - API 连接状态指示灯 (绿/红/灰) + 自动 /healthz 探测
  - SSE 连接状态指示灯 + DashboardPanel 的 EventSource 状态同步
  - 可展开日志面板 (最近50条，最近错误高亮)
  - 错误/警告/信息三级计数徽章
- DashboardPanel: EventSource onopen/onerror 通过 window 事件同步 SSE 状态
- App.css: 完整 StatusBar 样式 (fixed bottom bar, dark theme, 日志行 hover)

用户可通过底部状态栏一眼判断: API 通不通、SSE 连没连、最近什么错误。
